<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.studyweb.mapper.Relymapper">
<select id="Findbyknowtitle" parameterType="String" resultType="Rely">
    select * from tablerely where id=#{id} and old=#{knowtitle}
</select>
</mapper>